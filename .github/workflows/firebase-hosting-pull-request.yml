# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": pull_request
jobs:
  build_and_preview:
    if: "${{ github.event.pull_request.head.repo.full_name == github.repository }}"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      # Install Yarn dependencies
      - uses: actions/setup-node@v3.1.1
        with:
          node-version: "16.18.0"
      - run: npm install -g "yarn@1.22.5"
      - run: yarn install --immutable

      #Manual run
      - run: /opt/hostedtoolcache/node/16.18.0/x64/bin/npx firebase-tools@11.15.0 hosting:channel:deploy pr21-harrismcc_firestore- --expires 7d --project indoor-garden-b4585 --debug

      # Build
      # - run: yarn build
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: "${{ secrets.GITHUB_TOKEN }}"
      #     firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_INDOOR_GARDEN_B4585 }}"
      #     projectId: indoor-garden-b4585
      #     firebaseToolsVersion: "11.15.0"
